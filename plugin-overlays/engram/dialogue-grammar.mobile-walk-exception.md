<!--
CONSTITUTIONAL EXCEPTION — companion to learn-skill.mobile-walk-protocol.md.
A sibling of dialogue-grammar.checkpoint-exception.md, deliberately kept as
its own file rather than folded into it: each exception is paired with the
one protocol that licenses it, so either can be removed without leaving the
other's sentence stranded. This one sentence exists so the model is never
asked to adjudicate a live contradiction between the hard rule above and
the mobile-walk section in skills/learn/SKILL.md. Same licence, same pin
(checkDoctrine D5), same removal rule: if the mobile-walk protocol goes,
this goes in the same commit.
-->
**A second learner-elected exception to the menus rule:** when a sitting's opening message declares the mobile companion surface, `/learn`'s "Mobile walks — the card protocol (opt-in, companion surface only)" section governs how that sitting's beats may be served — per-sitting election, SELF-EXPLAIN and the carved-out VERIFY beats never a menu, capped ratings, `mobile-*` source stamps, and every node so encoded left provisional until a desk sitting solidifies it. Everywhere else, and by default everywhere, the rule above stands exactly as written.
